import { styled } from '@mui/material/styles';
import { ChromePicker } from 'react-color';

const Styles = {
    ChromePicker: styled(ChromePicker)({
        marginTop: '2rem',
        width: '100%'
    })
}

export default Styles